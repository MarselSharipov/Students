<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/item_add"
        android:icon="@drawable/ic_add"
        android:title="@null"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/item_sort_by_surname"
        android:title="@string/sort_by_surname"
        app:showAsAction="never" />

    <item
        android:id="@+id/item_sort_by_course"
        android:title="@string/sort_by_course"
        app:showAsAction="never" />

    <item
        android:id="@+id/item_sort_by_year"
        android:title="@string/sort_by_year"
        app:showAsAction="never" />

</menu>